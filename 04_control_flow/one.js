// === checks if the type is also same 
// where as == only checks for value

// we can use comma to stay in same scope but its a bad practice so dont use
if(true)console.log("test"), console.log("test2");